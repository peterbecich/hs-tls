# ~*~ auto-generated by haskell-ci with config : 612b7b1ece7f8fc31b366a09f7f526209ba27c62216380e24186ee1751a48093 ~*~

# Caching so the next build will be fast too.
cache:
  directories:
  - $HOME/.ghc
  - $HOME/.stack
  - $HOME/nix.store

# https://nixos.wiki/wiki/Nix_on_Travis
language: nix
os: linux
dist: bionic

jobs:
  include:
  - { env: BUILD=stack RESOLVER=ghc-8.0, addons: { apt: { packages: [ libgmp-dev, libssl-dev ] } } }
  - { env: BUILD=stack RESOLVER=ghc-8.2, addons: { apt: { packages: [ libgmp-dev, libssl-dev ] } } }
  - { env: BUILD=stack RESOLVER=ghc-8.4, addons: { apt: { packages: [ libgmp-dev, libssl-dev ] } } }
  - { env: BUILD=stack RESOLVER=ghc-8.6, addons: { apt: { packages: [ libgmp-dev, libssl-dev ] } } }
  - { env: BUILD=stack RESOLVER=ghc-8.6, addons: { apt: { packages: [ libgmp-dev, libssl-dev ] } }, os: osx }
  - { env: BUILD=stack RESOLVER=ghc-8.8, addons: { apt: { packages: [ libgmp-dev, libssl-dev ] } } }
  - { env: BUILD=hlint }
  - { env: BUILD=weeder, addons: { apt: { packages: [ libgmp-dev, libssl-dev ] } } }
  allow_failures:
  - { env: BUILD=hlint }
  - { env: BUILD=weeder, addons: { apt: { packages: [ libgmp-dev, libssl-dev ] } } }

before_install:
  - sudo mkdir -p /etc/nix
  - echo "substituters = https://cache.nixos.org/ file://$HOME/nix.store" | sudo tee -a /etc/nix/nix.conf > /dev/null
  - echo 'require-sigs = false' | sudo tee -a /etc/nix/nix.conf > /dev/null
  - nix-env -iA nixpkgs.stack

before_cache:
  - mkdir -p $HOME/nix.store
  - nix copy --to file://$HOME/nix.store -f default.nix buildInputs

before_script:
  - sudo mkdir -p /etc/nix && echo 'sandbox = true' | sudo tee /etc/nix/nix.conf

install:
  - export PATH=$HOME/.cabal/bin:$PATH

script:
- |
  set -ex
  if [ "x${RUNTEST}" = "xfalse" ]; then exit 0; fi
  case "$BUILD" in
    stack)
      # create the build stack.yaml
      case "$RESOLVER" in
      ghc-8.0)
        echo "{ resolver: lts-9.21, packages: [ core/, debug/, session/ ], extra-deps: [ memory-0.14.18, basement-0.0.8, cryptonite-0.27, x509-1.7.5, x509-system-1.6.6, x509-validation-1.6.11, x509-store-1.6.7 ], flags: {} }" > stack.yaml
        stack --no-terminal build --install-ghc --coverage --test --bench --no-run-benchmarks --haddock --no-haddock-deps
        ;;
      ghc-8.2)
        echo "{ resolver: lts-11.22, packages: [ core/, debug/, session/ ], extra-deps: [ memory-0.14.18, basement-0.0.8, cryptonite-0.27, x509-1.7.5, x509-system-1.6.6, x509-validation-1.6.11, x509-store-1.6.7 ], flags: {} }" > stack.yaml
        stack --no-terminal build --install-ghc --coverage --test --bench --no-run-benchmarks --haddock --no-haddock-deps
        ;;
      ghc-8.4)
        echo "{ resolver: lts-12.26, packages: [ core/, debug/, session/ ], extra-deps: [ cryptonite-0.27 ], flags: {} }" > stack.yaml
        stack --no-terminal build --install-ghc --coverage --test --bench --no-run-benchmarks --haddock --no-haddock-deps
        ;;
      ghc-8.6)
        echo "{ resolver: lts-14.27, packages: [ core/, debug/, session/ ], extra-deps: [ cryptonite-0.27 ], flags: {} }" > stack.yaml
        stack --no-terminal build --install-ghc --coverage --test --bench --no-run-benchmarks --haddock --no-haddock-deps
        ;;
      ghc-8.8)
        echo "{ resolver: lts-15.13, packages: [ core/, debug/, session/ ], extra-deps: [ cryptonite-0.27 ], flags: {} }" > stack.yaml
        stack --no-terminal build --install-ghc --coverage --test --bench --no-run-benchmarks --haddock --no-haddock-deps
        ;;
      esac
      stack exec ./.travis-post.sh
      ;;
    hlint)
      curl -sL https://raw.github.com/ndmitchell/hlint/master/misc/travis.sh | sh -s . --cpp-define=__GLASGOW_HASKELL__=800 --cpp-define=x86_64_HOST_ARCH=1 --cpp-define=mingw32_HOST_OS=1
      ;;
    weeder)
      stack --no-terminal build --install-ghc --ghc-options="-ddump-to-file -ddump-hi" --test --no-run-tests --bench --no-run-benchmarks
      curl -sL https://raw.github.com/ndmitchell/weeder/master/misc/travis.sh | sh -s .
      ;;
  esac
  set +ex

